server:
  port: 8080

spring:
  profiles:
    active: dev
  application:
    name: landslide-backend

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

jwt:
  secret: change-this-secret-in-production
  expire-hours: 12

chat:
  api:
    chat-url: ${CHAT_API_CHAT_URL:http://localhost:8001/chat}

ai:
  yolo:
    model-path: models/landslide-yolov8.onnx
    input-width: 640
    input-height: 640
    conf-threshold: 0.45
    nms-threshold: 0.45
    class-names:
      - landslide

rag:
  api:
    ask-url: ${RAG_API_ASK_URL:http://localhost:8000/ask}